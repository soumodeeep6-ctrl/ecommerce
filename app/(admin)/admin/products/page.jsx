"use client";

import { useEffect, useMemo, useState } from "react";
import Image from "next/image";
import Link from "next/link";
import { Search, Plus, Pencil, Trash2, PackageOpen, LoaderCircle } from "lucide-react";

const API = "https://api.escuelajs.co/api/v1";

export default function ProductsPage() {
  const [products, setProducts] = useState([]); const [search, setSearch] = useState(""); const [category, setCategory] = useState("all"); const [loading, setLoading] = useState(true); const [message, setMessage] = useState("");
  useEffect(() => { fetch(`${API}/products`).then((r) => r.json()).then(setProducts).catch(() => setMessage("Could not load products.")).finally(() => setLoading(false)); }, []);
  const categories = [...new Map(products.map((p) => [p.category?.id, p.category])).values()].filter(Boolean);
  const visible = useMemo(() => products.filter((p) => p.title.toLowerCase().includes(search.toLowerCase()) && (category === "all" || String(p.category?.id) === category)), [products, search, category]);
  async function removeProduct(id) { if (!window.confirm("Delete this product?")) return; const res = await fetch(`${API}/products/${id}`, { method: "DELETE" }); if (res.ok) { setProducts((items) => items.filter((p) => p.id !== id)); setMessage("Product deleted successfully."); } else setMessage("Could not delete product."); }

  return <div className="space-y-7"><header className="flex flex-col sm:flex-row sm:items-end justify-between gap-4"><div><p className="eyebrow text-[#e96546] mb-2">Catalog management</p><h1 className="display-type text-4xl font-bold">Products</h1><p className="text-[#777064] mt-2">Create, update, and remove products from your live catalog.</p></div><Link href="/admin/add" className="inline-flex items-center gap-2 rounded-full bg-[#2d2924] text-white px-5 py-3 text-sm font-semibold hover:bg-[#e96546]"><Plus size={17} /> Add product</Link></header><div className="bg-[#fbf8f2] border border-[#dfd7ca] rounded-2xl p-4 flex flex-col sm:flex-row gap-3"><div className="relative flex-1"><Search size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-[#a29a90]" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search products by name..." className="w-full rounded-xl border border-[#dfd7ca] bg-white py-3 pl-11 pr-4 outline-none focus:border-[#e96546]" /></div><select value={category} onChange={(e) => setCategory(e.target.value)} className="rounded-xl border border-[#dfd7ca] bg-white px-4 py-3 outline-none focus:border-[#e96546]"><option value="all">All categories</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div>{message && <p className="text-sm text-[#3f9d70]">{message}</p>}<div className="bg-[#fbf8f2] border border-[#dfd7ca] rounded-2xl overflow-hidden"><div className="px-5 py-4 border-b border-[#e5ded3] flex justify-between"><p className="font-semibold">All products <span className="text-[#a29a90] font-normal">({visible.length})</span></p><p className="text-xs text-[#a29a90]">Platzi Fake API</p></div>{loading ? <div className="p-16 flex justify-center"><LoaderCircle className="animate-spin text-[#e96546]" /></div> : <div className="overflow-x-auto"><table className="min-w-[760px] w-full text-sm"><thead><tr className="text-left text-[#777064] border-b border-[#e5ded3]"><th className="px-5 py-4 font-semibold">Product</th><th className="px-5 py-4 font-semibold">Category</th><th className="px-5 py-4 font-semibold">Price</th><th className="px-5 py-4 font-semibold">ID</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{visible.map((product) => <tr key={product.id} className="border-b border-[#eee8df] last:border-0 hover:bg-[#f8f5ef] transition"><td className="px-5 py-4"><div className="flex items-center gap-3"><Image src={product.images?.[0]} alt={product.title} width={48} height={48} className="w-12 h-12 rounded-xl object-cover" /><span className="font-semibold max-w-xs truncate">{product.title}</span></div></td><td className="px-5 py-4 text-[#777064]">{product.category?.name}</td><td className="px-5 py-4 font-bold">${product.price}</td><td className="px-5 py-4 text-[#a29a90]">#{product.id}</td><td className="px-5 py-4"><div className="flex justify-end gap-2"><Link href={`/admin/products/${product.id}/editproduct`} title="Edit" className="p-2 rounded-lg text-[#5c78d8] hover:bg-[#e9edfb]"><Pencil size={17} /></Link><button onClick={() => removeProduct(product.id)} title="Delete" className="p-2 rounded-lg text-[#d65b4c] hover:bg-[#fbe8e5]"><Trash2 size={17} /></button></div></td></tr>)}</tbody></table>{visible.length === 0 && <div className="p-16 text-center text-[#777064]"><PackageOpen className="mx-auto mb-3" /><p>No products match your filters.</p></div>}</div>}</div></div>;
}
